apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: get-custom-fields-fn-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/get-custom-fields
    rewrite:
      uri: /get-custom-fields
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80